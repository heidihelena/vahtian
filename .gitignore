.DS_Store
.claude/
# …but keep the committed agent skills (run-vahtian + UX review skills)
!.claude/
!.claude/skills/
!.claude/skills/**
# …and the copy-skill regression corpus the skills are gated against (CI runs it)
!.claude/evals/
!.claude/evals/**
# screenshots are large and regenerable — don't commit them
.claude/skills/run-vahtian/shots/
node_modules

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
__pycache__/
*.pyc

# Local Claude Code preview/launch config (machine-specific paths)
.claude/launch.json
